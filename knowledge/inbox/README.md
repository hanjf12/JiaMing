# 待摄取资料

把准备加入知识库的 Markdown、TXT 或 JSON 放在这里。每份资料至少注明：

- 标题与来源
- 作者、篇名或发布机构
- 获取日期
- 是否允许本地保存与再加工
- 哪些段落是原文，哪些是编辑备注

审核完成后，把结构化事实合并到 `raw/`，再运行 `npm run wiki:build` 和 `npm run wiki:lint`。本目录不会自动进入网页检索，避免未核验资料污染回答。
