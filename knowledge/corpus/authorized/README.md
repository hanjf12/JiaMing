# 授权文本导入区

仅把你有权保存和使用的文本放在这里。支持：

- JSON / JSONL：`title`、`author`、`content`，可选 `dynasty`、`source`、`license`
- TXT / Markdown：文件名作为作品名，全文作为内容

每份资料应同时记录授权来源。这里的内容仅供本机 Agent 通过只读 Shell 检索，不会编译进网页或上传到部署站点。
