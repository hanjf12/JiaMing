# 参与贡献

感谢参与嘉名。提交变更前请：

1. 使用 Node.js 22.13 或更高版本。
2. 不提交 `.env`、`config.local.json`、模型凭据、完整上游仓库或生成的 SQLite 文件。
3. 新增典籍内容时记录作品、篇名、作者、来源、核验状态和许可依据。
4. 不复制来源不明或仍受保护的近现代全文。
5. 运行：

```bash
npm run wiki:lint
npm test
```

代码应保持零第三方运行时依赖，除非新依赖带来的收益和许可证影响已经在变更说明中写清楚。面向用户的失败信息应使用简洁中文；日志不得输出 API Key、登录令牌或完整敏感资料。
