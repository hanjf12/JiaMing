# 嘉名：本地知识库与 AI 配置

## 直接使用（零依赖）

双击 `宝宝起名.html`。姓名生成、八字偏好排序、知识检索、收藏、导入知识和本地对话都在浏览器内完成，不需要安装任何软件，也不会发送数据。

本地知识库支持：

- 内置四书五经、唐诗、宋词、诗经、楚辞姓名记录与起名方法；
- TXT / Markdown 按空行分段导入；
- JSON 对象数组导入，字段为 `title`、`content`、`source`；
- 浏览器内加权检索，并在回答后列出命中的资料。

## 使用 Codex 订阅

前提：

1. 已安装 Codex CLI；
2. 运行过 `codex login`，并用 ChatGPT 账户登录；
3. 电脑有 Node.js 22（Codex 桌面版环境通常已包含）。

双击 `启动Codex订阅版.bat`，浏览器会打开 `http://127.0.0.1:4318/`。在“问典”的回答模式中选择“Codex 订阅（本机）”即可。

这条路径没有把 ChatGPT 订阅当作 API Key。页面只访问本机 `127.0.0.1` 服务，本机服务再调用已登录的 `codex exec`。每次回答使用只读沙箱、临时会话和临时输出文件；服务仅绑定本机回环地址，不应发布到公网。

如需检查登录状态：

```powershell
codex login status
```

如系统中的 `codex` 命令位置特殊，可在启动前设置 `CODEX_BIN` 为 CLI 的完整路径。

## 让手机或其他电脑访问

双击 `启动局域网版.bat`，然后让其他设备访问启动电脑的局域网 IPv4 地址和 `4318` 端口，例如：

```text
http://192.168.1.20:4318/
```

两台设备需要连接同一个家庭或可信局域网，Windows 防火墙也需要允许该端口。局域网设备可以使用取名、八字偏好排序、知识库检索与本地回答；为保护你的 Codex 订阅，远程设备默认不能调用 Codex AI。本机仍可通过 `http://127.0.0.1:4318/` 使用模型功能。

不要把 `4318` 端口映射到公网。若确有远程 AI 共享需求，应先增加身份验证、访问令牌、速率限制和 HTTPS，而不是直接设置端口转发。

## 使用 Ollama 或兼容接口

复制 `.env.example` 为 `.env.local`，按需修改：

```dotenv
AI_PROVIDER=ollama
AI_BASE_URL=http://127.0.0.1:11434
AI_MODEL=qwen3:4b
```

然后运行：

```powershell
pnpm dev
```

若使用 OpenAI 兼容接口，把 `AI_PROVIDER` 设为 `openai-compatible`，`AI_BASE_URL` 指向以 `/v1` 结尾的服务地址；如需密钥，再配置 `AI_API_KEY`。密钥只保存在服务端环境变量，不写入网页。

## 八字与隐私

- 程序只接受用户已经确认的四柱，不依据公历时间自动排盘；
- 用户需要自行指定“木、火、土、金、水”的用字倾向；
- 字的五行归类流派不一，本程序仅使用一个透明的常用字映射做候选排序；
- 不推算或声称已判断“喜用神”，不提供吉凶、健康、婚姻、财富等确定性结论；
- 八字默认不发送给模型，只有勾选“允许把八字与五行倾向发给本机模型”后才会进入请求。

## 官方参考

- [Codex 身份验证](https://developers.openai.com/codex/auth)
- [Codex CLI `exec` 命令](https://developers.openai.com/codex/cli/reference)
