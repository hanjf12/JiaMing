# 安全策略

## 报告问题

请不要在公开 Issue 中提交 API Key、Codex 登录信息、八字、出生时间或其他个人数据。维护者提供私密安全报告渠道后，应优先使用该渠道；在此之前可提交不含敏感细节的最小复现说明。

## 部署边界

嘉名设计为个人电脑上的本地服务，不是面向公网的多用户系统。它没有公网部署所需的账号体系、TLS 终止、持久限流、审计与密钥托管。

- 默认仅监听 `127.0.0.1`。
- `--lan` 只应在可信局域网中使用。
- `JIAMING_ALLOW_LAN_AGENT=true` 会允许其他设备消耗本机订阅或 API 额度。
- 不要把 `4318` 端口直接映射到公网。
- 定期检查 `.env`、`config.local.json` 和 Git 暂存区，避免提交凭据。
